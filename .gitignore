/target/*.jar
*.jar

# Ignore the server-information folder
server-information/

# Ignore all .zip files
*.zip

# Ignore IDE and configuration files
.vscode/

# Ignore specific microservices
eurecka/
server-information/
server-configuration/

# Real secret values must never be committed — fill in Secrets/app_bd-secret.yaml
# locally after this line takes effect; only the placeholder template is tracked.
Secrets/app_bd-secret.yaml

# Local docker-compose secrets — copy .env.example to .env and fill in real values
.env

# JMeter run artifacts (results, logs, generated HTML dashboards)
jmeter.log
jmeter/*.jtl
jmeter/report-*/
