# Leave empty to use Vite's dev proxy (see vite.config.ts) which forwards
# /api/* and /GymMonster/* to http://localhost:8081 (the gateway). Set this
# for production builds pointing at a deployed backend, e.g.:
# VITE_API_BASE_URL=https://api.monstergym.com
VITE_API_BASE_URL=
