apiVersion: v1
kind: ConfigMap
metadata:
  name: svc-admin-map
data:
  # Consumed directly via configMapKeyRef in server-administrator-dep.yaml.
  server.port-dev: "8900"
  info-server-dev: "server-information"
  info-server-uri-dev: "lb://server-information"
