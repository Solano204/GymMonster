apiVersion: v1
kind: ConfigMap
metadata:
  name: svc-register-map
data:
  # Consumed directly via configMapKeyRef in server-register-dep.yaml.
  server.port-dev: "8902"
  r2dbc-url-dev: "r2dbc:mysql://mysql-server:3306/mscv_information"
  kafka_host-dev: "kafka-service:9092"
