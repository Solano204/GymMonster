# TEMPLATE ONLY — do not put real values here and do not commit real values.
# base64 is *encoding*, not encryption: anything in `data:` below is fully
# readable by anyone with this file. Fill in real secrets locally only,
# after this file is listed in .gitignore, or generate the Secret directly
# with `kubectl create secret generic app-secrets --from-literal=KEY=value`
# instead of applying this file.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
data:
  MYSQL_USERNAME: Y2hhbmdlbWU=       # 'changeme' — replace locally, do not commit real value
  MYSQL_PASSWORD: Y2hhbmdlbWU=       # 'changeme' — replace locally, do not commit real value
  REDIS_PASSWORD: Y2hhbmdlbWU=       # 'changeme' — replace locally, do not commit real value
  EMAIL_PASSWORD: Y2hhbmdlbWU=       # 'changeme' — replace locally, do not commit real value
  ADMIN_SECRET: Y2hhbmdlbWU=        # 'changeme' — replace locally, do not commit real value
  PASSWORD-KEY: Y2hhbmdlbWU=         # 'changeme' — replace locally, do not commit real value
