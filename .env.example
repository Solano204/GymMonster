# Copy to .env and fill in real local values. .env is gitignored — never commit it.
MYSQL_ROOT_PASSWORD=changeme
KEYCLOAK_DB_PASSWORD=changeme
KEYCLOAK_ADMIN_PASSWORD=changeme
REDIS_PASSWORD=changeme
